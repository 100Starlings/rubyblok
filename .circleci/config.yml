version: 2.1

orbs:
  slack: circleci/slack@4.4.4

jobs:
  build:
    docker:
      - image: cimg/ruby:3.3.0

    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            gem install bundler
            bundle install --jobs=4 --retry=3
      - run:
          name: RuboCop
          command: |
            bundle exec rubocop
      - run:
          name: RSpec
          command: |
            bundle exec rspec

      - slack/notify:
          channel: $SLACK_DEFAULT_CHANNEL
          event: always
          message: "Build finished: $CIRCLE_BUILD_URL"

workflows:
  version: 2
  build:
    jobs:
      - build
